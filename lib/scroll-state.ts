/**
 * Shared mutable scroll state, written by GSAP ScrollTrigger (Hero)
 * and read inside R3F's useFrame without triggering React re-renders.
 */
export const heroScroll = { progress: 0 };

export const servicesScroll = { progress: 0 };
